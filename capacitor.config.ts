import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.physioapp.app',
  appName: 'Health Hub',
  webDir: 'dist/physio-app/browser'
};

export default config;
